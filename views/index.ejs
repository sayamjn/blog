<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900">
    <header class="bg-blue-600 text-white py-4">
        <h1 class="text-center text-2xl font-bold">Blog Manager</h1>
    </header>
    <main class="max-w-4xl mx-auto mt-8">
        <section class="mb-8">
            <h2 class="text-xl font-semibold mb-4">All Blogs</h2>
            <% if (data && data.length > 0) { %>
                <% data.forEach((blog) => { %>
                    <div class="bg-white shadow-md rounded-lg p-4 mb-4">
                        <h3 class="text-lg font-bold mb-2">
                            <a href="/<%= blog.title %>" class="text-blue-500 hover:underline"><%= blog.title %></a>
                        </h3>
                        <p class="text-gray-700 mb-4"><%= blog.content.substring(0, 100) %>...</p>
                        <div class="flex space-x-4">
                            <form action="/<%= blog._id %>?_method=DELETE" method="POST">
                                <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                                    Delete
                                </button>
                            </form>
                            <form action="/<%= blog._id %>" method="GET">
                                <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                                    Edit
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-gray-500">No blogs available. Create one below!</p>
            <% } %>
        </section>

        <section class="bg-white shadow-md rounded-lg p-6">
            <h2 class="text-xl font-semibold mb-4">Create New Blog</h2>
            <form action="/" method="POST" class="space-y-4">
                <div>
                    <label for="title" class="block text-sm font-medium">Title</label>
                    <input type="text" id="title" name="title" required
                           class="w-full border border-gray-300 rounded px-3 py-2">
                </div>
                <div>
                    <label for="content" class="block text-sm font-medium">Content</label>
                    <textarea id="content" name="content" rows="5" required
                              class="w-full border border-gray-300 rounded px-3 py-2"></textarea>
                </div>
                <button type="submit"
                        class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">
                    Add Blog
                </button>
            </form>
        </section>
    </main>
</body>
</html>
